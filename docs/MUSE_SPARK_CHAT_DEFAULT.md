# ClutchBot chat default → muse-spark-1.3

**HOLD.** Empty shell for Qorector to build.

- Change ClutchBot chat LLM default from `glm-5.3-flash` to `muse-spark-1.3` (Quicksilver).
- Confirm / scoreboard VLM pin is **out of scope** unless the operator says otherwise.
- DualSense stays on the PS5.
- Do not merge until GO MERGE in Qorector chat.
